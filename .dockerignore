# Dependencies & build output (rebuilt inside the image)
node_modules
server

# Local runtime data & secrets (provided at runtime via volumes).
# Keep offline.png so it can seed the uploads volume; drop per-guild data.
uploads
!uploads/offline.png
configs.json
env.conf

# VCS / editor / OS noise
.git
.gitignore
.vscode
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Docker assets themselves
Dockerfile
docker-compose.yml
.dockerignore
